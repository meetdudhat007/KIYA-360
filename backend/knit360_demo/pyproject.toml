[project]
name = "knit360_demo"
authors = [{ name = "KNIT 360", email = "dev@knit360.local" }]
description = "Demo seeding over ERPNext and HRMS. Not the KNIT 360 product."
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"
