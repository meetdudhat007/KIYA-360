[project]
name = "knit_core"
authors = [{ name = "KNIT", email = "dev@knit.local" }]
description = "KNIT platform. Built to the BRD on the Frappe framework."
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = ">=15.0.0,<16.0.0"
