[project]
name = "knit_demo"
authors = [{ name = "KNIT", email = "dev@knit.local" }]
description = "Demo seeding over ERPNext and HRMS. Not the KNIT product."
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"
